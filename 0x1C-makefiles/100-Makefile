# Compiles the C program
CC = gcc
CFLAGS = -Wall -Werror -Wextra -pedantic

NAME = school

all: $(NAME)

$(NAME): $(OBJ)
	$(CC) $(CFLAGS) -o $@ $^

%.o: %.c m.h
	$(CC) $(CFLAGS) -c $< -o $@

clean:
	rm -f $(NAME) *.o *.a

oclean:
	rm -f *.o

fclean: clean oclean

re: fclean all
