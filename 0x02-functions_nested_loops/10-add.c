#include "main.h"
/* A function that adds two integers and returns the result */

	/**
	* add function - give the addition of 2 numbers
	* takes towo intergers as agument
	*
	* returns the sum two integers
	*/int add(int a, int b)
{
	int result;

	result = a + b;
	_putchar('0' + result);
	return (result);
}
